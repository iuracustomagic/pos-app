const constrants = Object.freeze({
  itemsToDisplay: 15,
  BACKENDURL: "http://localhost",
  SERVER_PORT: 5500,
  toggleModalInterval: 3000,
  carouselInterval: 3000,
  timeToQuit: 900000,
  timeToScan: 1500,
  filterDiscount: {
    guarantee: 0,
    date: -1,
    regular: -2
  }
});

export default constrants;
